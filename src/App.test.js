import { render, screen } from "@testing-library/react";
import App from "./App";

test("renders learn react link", () => {
  const gt = 4;
  expect(gt).toEqual(4);
});

test("renders learn react link", () => {
  const gt = 4;
  expect(gt).toEqual(4);
});
